import styled from "styled-components";
import { space, color, flexbox, layout, position } from "styled-system";

const Box = styled.div`
  ${color}
  ${flexbox}
  ${layout}
  ${position}
  ${space}
`;

export default Box;
